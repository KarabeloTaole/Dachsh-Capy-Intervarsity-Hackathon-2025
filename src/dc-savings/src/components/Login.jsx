const handleSubmit = () =>{
    if(!FormData.username || !FormData.password){
        alert("Please fill in all fields")
    } else {
        // proceed with login
        setTimeout(() => {
            
    }
}